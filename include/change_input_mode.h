#ifndef _CHANGE_INPUT_MODE_H
#define _CHANGE_INPUT_MODE_H
#include <iostream>

/**
*   Function to modify the existing "input to get to test" for a selected tester
*/
void change_input_mode(std::string tester_name);

#endif