#ifndef _ADD_TESTS_MODE_H
#define _ADD_TESTS_MODE_H

#include <iostream>

/**
*   Function to add tests to an existing tester
*
*   @tester_name: the name of the tester we are adding a test to
*/
void add_tests_mode(std::string tester_name);

#endif